import React from "react";

import "./header.css";

const Header = () => {
  return (
    <header className="main-header">
      <h2>React Reusable Components</h2>
    </header>
  );
};

export default Header;
